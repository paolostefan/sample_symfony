{% extends 'base.html.twig' %}

{% block title %}Edit Poi{% endblock %}

{% block body %}
  <h1>Edit Poi</h1>

  {{ include('poi/_form.html.twig', {'button_label': 'Update'}) }}

  <div class="row m-t-10">
    <div class="col-md-2"><a class="btn bg-grey my-1" href="{{ path('poi_index') }}">Back to list</a></div>
    <div class="col-md-2 col-md-offset-8 text-right">{{ include('poi/_delete_form.html.twig') }}</div>
  </div>
{% endblock %}
